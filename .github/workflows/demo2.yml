name: GA_Demo2
on: 
  # 下記のWFがデフォルトブランチで実行されないと動かない
  workflow_run:
    workflows: [GA_Demo]
    types: [completed]

jobs:
  two:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"